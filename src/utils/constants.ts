export const FRONTEND_URL = process?.env?.FRONTEND_URL ?? "https://snippetsearcherdev.westus2.cloudapp.azure.com"
export const BACKEND_URL = process?.env?.BACKEND_URL ?? "https://snippetsearcherdev.westus2.cloudapp.azure.com/api"
export const AUTH0_USERNAME = process?.env?.AUTH0_USERNAME ?? ""
export const AUTH0_PASSWORD = process?.env?.AUTH0_PASSWORD ?? ""
